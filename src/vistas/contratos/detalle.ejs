<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle del Contrato - CARSIL Equipos y Servicios SAC</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { 
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #f8fafc 0%, #e9ecef 100%);
            min-height: 100vh;
        }
        
        .sidebar, .main-content { transition: all 0.3s; }
        
        .toggle-sidebar-btn {
            position: absolute;
            top: 1.2rem;
            left: 290px;
            z-index: 1100;
            background: linear-gradient(135deg, #0056b3, #003366);
            color: #fff;
            border: none;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 12px rgba(0,86,179,0.3);
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .toggle-sidebar-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(0,86,179,0.4);
        }
        
        .sidebar.hide { transform: translateX(-100%); }
        .main-content.full { margin-left: 0 !important; }
        
        @media (max-width: 768px) {
            .toggle-sidebar-btn { left: 10px; top: 10px; }
        }
        
        .sidebar {
            width: 270px;
            min-height: 100vh;
            background: linear-gradient(180deg, #003366 0%, #001a33 100%);
            color: #fff;
            position: fixed;
            left: 0;
            top: 0;
            padding-top: 1rem;
            z-index: 1000;
            box-shadow: 4px 0 20px rgba(0,0,0,0.1);
        }
        
        .sidebar .sidebar-heading {
            font-size: 1.5rem;
            font-weight: 700;
            letter-spacing: 2px;
            color: #fff;
        }
        
        .sidebar .company-slogan {
            font-size: 0.9rem;
            color: #b0c4de;
            font-weight: 300;
        }
        
        .sidebar .menu-item {
            color: rgba(255,255,255,0.9);
            padding: 0.8rem 1.2rem;
            border-radius: 0.7rem;
            margin: 0.2rem 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.8rem;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s;
            position: relative;
        }
        
        .sidebar .menu-item:hover {
            background: rgba(0,86,179,0.3);
            color: #fff;
            transform: translateX(5px);
        }
        
        .sidebar .menu-item.active {
            background: linear-gradient(135deg, #0056b3, #0d6efd);
            color: #fff;
            box-shadow: 0 2px 8px rgba(0,86,179,0.3);
        }
        
        .sidebar .logout-item {
            color: #ff6b6b;
        }
        
        .sidebar .logout-item:hover {
            background: rgba(255,107,107,0.1);
            color: #ff6b6b;
        }
        
        .main-content {
            margin-left: 270px;
            min-height: 100vh;
        }
        
        .user-profile {
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }
        
        .user-avatar {
            background: linear-gradient(135deg, #0056b3, #003366);
            color: #fff;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            font-size: 1.2rem;
            box-shadow: 0 2px 8px rgba(0,86,179,0.3);
        }
        
        .navbar-top {
            background: rgba(255,255,255,0.95) !important;
            backdrop-filter: blur(10px);
            box-shadow: 0 2px 20px rgba(0,0,0,0.08);
            border: none !important;
        }
        
        .estado-badge {
            padding: 0.4rem 0.8rem;
            border-radius: 1rem;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .estado-activo { 
            background: linear-gradient(135deg, rgba(40,167,69,0.1), rgba(40,167,69,0.05));
            color: #28a745; 
            border: 1px solid rgba(40,167,69,0.2);
        }
        .estado-finalizado { 
            background: linear-gradient(135deg, rgba(0,86,179,0.1), rgba(0,86,179,0.05));
            color: #0056b3; 
            border: 1px solid rgba(0,86,179,0.2);
        }
        .estado-suspendido { 
            background: linear-gradient(135deg, rgba(255,193,7,0.1), rgba(255,193,7,0.05));
            color: #ffc107; 
            border: 1px solid rgba(255,193,7,0.2);
        }
        .estado-cancelado { 
            background: linear-gradient(135deg, rgba(220,53,69,0.1), rgba(220,53,69,0.05));
            color: #dc3545; 
            border: 1px solid rgba(220,53,69,0.2);
        }
        
        .card {
            border: none;
            border-radius: 1.2rem;
            box-shadow: 0 8px 32px rgba(0,0,0,0.08);
            overflow: hidden;
            background: rgba(255,255,255,0.95);
            backdrop-filter: blur(10px);
        }
        
        .card-header {
            background: linear-gradient(135deg, #fff 0%, #f8fafc 100%);
            border-bottom: 2px solid rgba(0,86,179,0.1);
            padding: 1.5rem 2rem;
            position: relative;
        }
        
        .card-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #0056b3, #0d6efd, #6f42c1);
        }
        
        .card-header h5, .card-header h6 {
            color: #003366;
            font-weight: 700;
            margin-bottom: 0;
        }
        
        .info-item {
            padding: 1.5rem;
            background: linear-gradient(135deg, #f8fafc 0%, #ffffff 100%);
            border-radius: 1rem;
            margin-bottom: 1.5rem;
            border: 1px solid rgba(0,86,179,0.1);
            border-left: 4px solid #0056b3;
            transition: all 0.3s;
        }
        
        .info-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
        }
        
        .info-item h6 {
            color: #003366;
            margin-bottom: 0.8rem;
            font-weight: 700;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .info-item p {
            margin-bottom: 0;
            color: #495057;
            font-weight: 500;
        }
        
        .btn-modern {
            border-radius: 0.8rem;
            padding: 0.8rem 2rem;
            font-weight: 600;
            border: none;
            transition: all 0.3s;
            display: inline-flex;
            align-items: center;
            gap: 0.6rem;
            text-decoration: none;
        }
        
        .btn-primary-modern {
            background: linear-gradient(135deg, #0056b3, #0d6efd);
            color: #fff;
            box-shadow: 0 4px 15px rgba(0,86,179,0.3);
        }
        
        .btn-primary-modern:hover {
            background: linear-gradient(135deg, #003d82, #0b5ed7);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,86,179,0.4);
            color: #fff;
        }
        
        .btn-warning-modern {
            background: linear-gradient(135deg, #ffc107, #fd7e14);
            color: #fff;
            box-shadow: 0 4px 15px rgba(255,193,7,0.3);
        }
        
        .btn-warning-modern:hover {
            background: linear-gradient(135deg, #e0a800, #dc6545);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(255,193,7,0.4);
            color: #fff;
        }
        
        .btn-secondary-modern {
            background: linear-gradient(135deg, #6c757d, #495057);
            color: #fff;
            box-shadow: 0 4px 15px rgba(108,117,125,0.3);
        }
        
        .btn-secondary-modern:hover {
            background: linear-gradient(135deg, #5a6268, #343a40);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(108,117,125,0.4);
            color: #fff;
        }
        
        .timeline-item {
            position: relative;
            padding-left: 2.5rem;
            margin-bottom: 1.5rem;
            padding: 1.2rem 1.2rem 1.2rem 2.5rem;
            background: linear-gradient(135deg, #f8fafc 0%, #ffffff 100%);
            border-radius: 0.8rem;
            border: 1px solid rgba(0,86,179,0.1);
            transition: all 0.3s;
        }
        
        .timeline-item:hover {
            transform: translateX(5px);
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        
        .timeline-item::before {
            content: '';
            position: absolute;
            left: 1rem;
            top: 1.5rem;
            width: 12px;
            height: 12px;
            background: linear-gradient(135deg, #0056b3, #0d6efd);
            border-radius: 50%;
            border: 3px solid #fff;
            box-shadow: 0 0 0 3px rgba(0,86,179,0.2);
        }
        
        .timeline-item::after {
            content: '';
            position: absolute;
            left: 1.35rem;
            top: 2.2rem;
            width: 2px;
            height: calc(100% + 1rem);
            background: linear-gradient(180deg, rgba(0,86,179,0.3), rgba(0,86,179,0.1));
            z-index: -1;
        }
        
        .timeline-item:last-child::after {
            display: none;
        }
        
        .page-header {
            background: linear-gradient(135deg, #fff 0%, #f8fafc 100%);
            border-radius: 1.2rem;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            border: 1px solid rgba(0,86,179,0.1);
            position: relative;
            overflow: hidden;
        }
        
        .page-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #0056b3, #0d6efd, #6f42c1);
        }
        
        .page-title {
            color: #003366;
            font-weight: 700;
            font-size: 2rem;
            margin-bottom: 0.5rem;
        }
        
        .page-subtitle {
            color: #6c757d;
            margin-bottom: 0;
        }
        
        .alert {
            border: none;
            border-radius: 0.8rem;
            padding: 1rem 1.5rem;
            margin-bottom: 1.5rem;
        }
        
        .alert-danger {
            background: linear-gradient(135deg, rgba(220,53,69,0.1), rgba(220,53,69,0.05));
            color: #721c24;
            border-left: 4px solid #dc3545;
        }
        
        .alert-success {
            background: linear-gradient(135deg, rgba(40,167,69,0.1), rgba(40,167,69,0.05));
            color: #155724;
            border-left: 4px solid #28a745;
        }
        
        .floating-shapes {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            overflow: hidden;
        }
        
        .shape {
            position: absolute;
            opacity: 0.05;
            animation: float 6s ease-in-out infinite;
        }
        
        .shape:nth-child(1) {
            top: 20%;
            left: 10%;
            animation-delay: 0s;
        }
        
        .shape:nth-child(2) {
            top: 60%;
            right: 10%;
            animation-delay: 2s;
        }
        
        .shape:nth-child(3) {
            bottom: 20%;
            left: 50%;
            animation-delay: 4s;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }
    </style>
</head>
<body>

    <div class="wrapper">
        <!-- Sidebar -->
        <nav class="sidebar" id="sidebar">
            <div class="sidebar-heading text-center py-4">
                <div class="company-name">CARSIL</div>
                <div class="company-slogan">Equipos y Servicios SAC</div>
            </div>
            <div class="d-flex flex-column h-100 px-2 py-3">
                <a href="/menu/principal" class="menu-item">
                    <i class="bi bi-house"></i>
                    Inicio
                </a>
                <a href="/proformas" class="menu-item">
                    <i class="bi bi-file-earmark-text"></i>
                    Proformas
                </a>
                <a href="/facturas" class="menu-item">
                    <i class="bi bi-receipt"></i>
                    Facturas
                </a>
                    <i class="bi bi-file-earmark-check"></i>
                </a>
                <a href="/clientes" class="menu-item">
                    <i class="bi bi-people"></i>
                    Clientes
                </a>
                <a href="/productos" class="menu-item">
                    <i class="bi bi-box"></i>
                    Productos
                </a>
                <a href="/empleados" class="menu-item">
                    <i class="bi bi-person-badge"></i>
                    Empleados
                </a>
                <a href="/pagos" class="menu-item">
                    <i class="bi bi-cash-coin"></i>
                    Pagos
                </a>
                <a href="/reportes" class="menu-item">
                    <i class="bi bi-bar-chart"></i>
                    Reportes
                </a>
                <a href="/usuarios" class="menu-item">
                    <i class="bi bi-person-circle"></i>
                    Usuarios
                </a>
                <a href="/roles" class="menu-item">
                    <i class="bi bi-shield-lock"></i>
                    Roles
                </a>
                <a href="/asistencia" class="menu-item">
                    <i class="bi bi-calendar-check"></i>
                    Asistencias
                </a>
                <a href="/empresa" class="menu-item">
                    <i class="bi bi-gear"></i>
                    Empresa
                </a>
                <div class="mt-auto">
                    <a href="/login" class="menu-item logout-item">
                        <i class="bi bi-box-arrow-right"></i>
                        Cerrar Sesión
                    </a>
                </div>
            </div>
        </nav>

        <!-- Botón para ocultar/mostrar barra -->
        <button class="toggle-sidebar-btn" id="toggleSidebarBtn" title="Ocultar/Mostrar menú">
            <i class="bi bi-chevron-left" id="toggleIcon"></i>
        </button>

        <!-- Main Content -->
        <div class="main-content" id="main-content">
            <nav class="navbar navbar-expand-lg navbar-light navbar-top border-bottom">
                <div class="container-fluid">
                    <h5 class="navbar-brand ms-3 mb-0">
                        <i class="bi bi-file-earmark-check me-2"></i>
                        Detalle del Contrato
                    </h5>
                    <div class="user-profile ms-auto">
                        <span><%= user && user.Nombres ? user.Nombres : 'Administrador' %></span>
                        <div class="user-avatar"><%= user && user.Nombres ? user.Nombres.charAt(0) : 'A' %></div>
                    </div>
                </div>
            </nav>


            <div class="container-fluid py-4">
                <!-- Header modernizado -->
                <div class="page-header">
                    <div class="floating-shapes">
                        <i class="bi bi-file-earmark-check shape" style="font-size: 3rem;"></i>
                        <i class="bi bi-person-badge shape" style="font-size: 2.5rem;"></i>
                        <i class="bi bi-receipt shape" style="font-size: 2rem;"></i>
                    </div>
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h1 class="page-title">
                                <i class="bi bi-file-earmark-check me-3 text-primary"></i>
                                Contrato <%= contrato.Codigo %>
                            </h1>
                            <p class="page-subtitle">Detalles completos del contrato y información relacionada</p>
                        </div>
                        <div class="d-flex align-items-center gap-3">
                            <span class="estado-badge estado-<%= contrato.Estado.toLowerCase() %>">
                                <%= contrato.Estado %>
                            </span>
                                <i class="bi bi-arrow-left"></i>
                                Volver al Listado
                            </a>
                        </div>
                    </div>
                </div>
                <!-- Mensajes flash -->
                <% if (error) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        <%= error %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                <% } %>
                <% if (success) { %>
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <i class="bi bi-check-circle me-2"></i>
                        <%= success %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                <% } %>

        <div class="row">
            <!-- Información Principal -->
            <div class="col-lg-8">
                <div class="card shadow-sm mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-info-circle me-2"></i>
                            Información General
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="info-item">
                                    <h6><i class="bi bi-hash me-2"></i>Código del Contrato</h6>
                                    <p><strong><%= contrato.Codigo %></strong></p>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <h6><i class="bi bi-calendar-event me-2"></i>Fecha de Registro</h6>
                                    <p><%= new Date(contrato.FechaRegistro).toLocaleDateString('es-PE', {
                                        year: 'numeric',
                                        month: 'long',
                                        day: 'numeric',
                                        hour: '2-digit',
                                        minute: '2-digit'
                                    }) %></p>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <h6><i class="bi bi-calendar-check me-2"></i>Fecha de Inicio</h6>
                                    <p><strong><%= new Date(contrato.FechaInicio).toLocaleDateString('es-PE') %></strong></p>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <h6><i class="bi bi-calendar-x me-2"></i>Fecha de Fin</h6>
                                    <p>
                                        <% if (contrato.FechaFin) { %>
                                            <strong><%= new Date(contrato.FechaFin).toLocaleDateString('es-PE') %></strong>
                                        <% } else { %>
                                            <span class="text-muted">Indefinido</span>
                                        <% } %>
                                    </p>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <h6><i class="bi bi-currency-dollar me-2"></i>Pago Semanal</h6>
                                    <p>
                                        <% if (contrato.PagoSemanal) { %>
                                            <strong class="text-success">S/ <%= parseFloat(contrato.PagoSemanal).toFixed(2) %></strong>
                                        <% } else { %>
                                            <span class="text-muted">No definido</span>
                                        <% } %>
                                    </p>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <h6><i class="bi bi-bank me-2"></i>Cuenta Bancaria</h6>
                                    <p>
                                        <% if (contrato.NumeroCuentaBanco) { %>
                                            <%= contrato.NumeroCuentaBanco %>
                                        <% } else { %>
                                            <span class="text-muted">No especificada</span>
                                        <% } %>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Información del Cliente -->
                <div class="card shadow-sm mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-person-badge me-2"></i>
                            Información del Cliente
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="info-item">
                                    <h6><i class="bi bi-building me-2"></i>Razón Social</h6>
                                    <p><strong><%= contrato.ClienteNombre %></strong></p>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <h6><i class="bi bi-card-text me-2"></i>Documento</h6>
                                    <p><%= contrato.ClienteDocumento %></p>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <h6><i class="bi bi-envelope me-2"></i>Email</h6>
                                    <p>
                                        <% if (contrato.ClienteEmail) { %>
                                            <a href="mailto:<%= contrato.ClienteEmail %>"><%= contrato.ClienteEmail %></a>
                                        <% } else { %>
                                            <span class="text-muted">No especificado</span>
                                        <% } %>
                                    </p>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <h6><i class="bi bi-telephone me-2"></i>Teléfono</h6>
                                    <p>
                                        <% if (contrato.ClienteTelefono) { %>
                                            <%= contrato.ClienteTelefono %>
                                        <% } else { %>
                                            <span class="text-muted">No especificado</span>
                                        <% } %>
                                    </p>
                                </div>
                            </div>
                            <div class="col-12">
                                <div class="info-item">
                                    <h6><i class="bi bi-geo-alt me-2"></i>Dirección</h6>
                                    <p>
                                        <% if (contrato.ClienteDireccion) { %>
                                            <%= contrato.ClienteDireccion %>
                                        <% } else { %>
                                            <span class="text-muted">No especificada</span>
                                        <% } %>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Información de la Factura -->
                <% if (contrato.FacturaCodigo) { %>
                <div class="card shadow-sm mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-receipt me-2"></i>
                            Factura Asociada
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-4">
                                <div class="info-item">
                                    <h6><i class="bi bi-hash me-2"></i>Código</h6>
                                    <p><strong><%= contrato.FacturaCodigo %></strong></p>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="info-item">
                                    <h6><i class="bi bi-calendar me-2"></i>Fecha Emisión</h6>
                                    <p><%= new Date(contrato.FacturaFecha).toLocaleDateString('es-PE') %></p>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="info-item">
                                    <h6><i class="bi bi-currency-dollar me-2"></i>Total</h6>
                                    <p><strong class="text-success">S/ <%= parseFloat(contrato.FacturaTotal).toFixed(2) %></strong></p>
                                </div>
                            </div>
                        </div>
                        <div class="mt-3">
                            <a href="/facturas/<%= contrato.IdFactura %>" class="btn btn-outline-primary btn-modern">
                                <i class="bi bi-eye"></i>
                                Ver Factura Completa
                            </a>
                        </div>
                    </div>
                </div>
                <% } %>

                <!-- Términos y Condiciones -->
                <% if (contrato.Terminos) { %>
                <div class="card shadow-sm">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-file-text me-2"></i>
                            Términos y Condiciones
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="info-item">
                            <p style="white-space: pre-wrap;"><%= contrato.Terminos %></p>
                        </div>
                    </div>
                </div>
                <% } %>
            </div>

            <!-- Panel de Acciones -->
            <div class="col-lg-4">
                <div class="card shadow-sm mb-4">
                    <div class="card-header">
                        <h6 class="mb-0">
                            <i class="bi bi-gear me-2"></i>
                            Acciones del Contrato
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="d-grid gap-2">
                            <a href="/contratos/<%= contrato.IdContrato %>/editar" class="btn btn-warning-modern btn-modern">
                                <i class="bi bi-pencil"></i>
                                Editar Contrato
                            </a>
                            
                            <% if (contrato.Estado === 'ACTIVO') { %>
                            <button type="button" class="btn btn-outline-secondary btn-modern btn-cambiar-estado" 
                                    data-id="<%= contrato.IdContrato %>" data-estado="SUSPENDIDO">
                                <i class="bi bi-pause"></i>
                                Suspender Contrato
                            </button>
                            <button type="button" class="btn btn-outline-primary btn-modern btn-cambiar-estado" 
                                    data-id="<%= contrato.IdContrato %>" data-estado="FINALIZADO">
                                <i class="bi bi-flag"></i>
                                Finalizar Contrato
                            </button>
                            <% } else if (contrato.Estado === 'SUSPENDIDO') { %>
                            <button type="button" class="btn btn-outline-success btn-modern btn-cambiar-estado" 
                                    data-id="<%= contrato.IdContrato %>" data-estado="ACTIVO">
                                <i class="bi bi-play"></i>
                                Reactivar Contrato
                            </button>
                            <% } %>
                            
                            <hr>
                            
                            <button type="button" class="btn btn-outline-danger btn-modern btn-eliminar" 
                                    data-id="<%= contrato.IdContrato %>" data-codigo="<%= contrato.Codigo %>">
                                <i class="bi bi-trash"></i>
                                Eliminar Contrato
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Timeline -->
                <div class="card shadow-sm">
                    <div class="card-header">
                        <h6 class="mb-0">
                            <i class="bi bi-clock-history me-2"></i>
                            Cronología del Contrato
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="timeline-item">
                            <div>
                                <strong>Contrato Creado</strong>
                                <br>
                                <small class="text-muted">
                                    <%= new Date(contrato.FechaRegistro).toLocaleDateString('es-PE', {
                                        year: 'numeric',
                                        month: 'short',
                                        day: 'numeric',
                                        hour: '2-digit',
                                        minute: '2-digit'
                                    }) %>
                                </small>
                            </div>
                        </div>
                        
                        <div class="timeline-item">
                            <div>
                                <strong>Fecha de Inicio</strong>
                                <br>
                                <small class="text-muted">
                                    <%= new Date(contrato.FechaInicio).toLocaleDateString('es-PE', {
                                        year: 'numeric',
                                        month: 'short',
                                        day: 'numeric'
                                    }) %>
                                </small>
                            </div>
                        </div>
                        
                        <% if (contrato.FechaFin) { %>
                        <div class="timeline-item">
                            <div>
                                <strong>Fecha de Finalización</strong>
                                <br>
                                <small class="text-muted">
                                    <%= new Date(contrato.FechaFin).toLocaleDateString('es-PE', {
                                        year: 'numeric',
                                        month: 'short',
                                        day: 'numeric'
                                    }) %>
                                </small>
                            </div>
                        </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
                </div>
            </div>
        </div>
    </div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Sidebar functionality
    const toggleBtn = document.getElementById('toggleSidebarBtn');
    const sidebar = document.getElementById('sidebar');
    const mainContent = document.getElementById('main-content');
    const toggleIcon = document.getElementById('toggleIcon');
    const userAvatar = document.querySelector('.user-avatar');
    const userName = '<%= user && user.Nombres ? user.Nombres : "Administrador" %>';
    
    if (userAvatar) {
        userAvatar.textContent = userName.charAt(0);
    }

    // Toggle sidebar
    if (toggleBtn) {
        toggleBtn.addEventListener('click', function() {
            sidebar.classList.toggle('hide');
            mainContent.classList.toggle('full');
            if (sidebar.classList.contains('hide')) {
                toggleIcon.classList.remove('bi-chevron-left');
                toggleIcon.classList.add('bi-chevron-right');
            } else {
                toggleIcon.classList.remove('bi-chevron-right');
                toggleIcon.classList.add('bi-chevron-left');
            }
        });
    }
    // Cambiar estado
    document.querySelectorAll('.btn-cambiar-estado').forEach(btn => {
        btn.addEventListener('click', function() {
            const id = this.dataset.id;
            const nuevoEstado = this.dataset.estado;
            const accion = getAccionTexto(nuevoEstado);
            
            if (confirm(`¿Está seguro de que desea ${accion} este contrato?`)) {
                cambiarEstadoContrato(id, nuevoEstado);
            }
        });
    });
    
    // Eliminar contrato
    document.querySelector('.btn-eliminar').addEventListener('click', function() {
        const id = this.dataset.id;
        const codigo = this.dataset.codigo;
        
        if (confirm(`¿Está seguro de que desea eliminar el contrato ${codigo}?\n\nEsta acción no se puede deshacer.`)) {
            eliminarContrato(id);
        }
    });
    
    function getAccionTexto(estado) {
        switch(estado) {
            case 'ACTIVO': return 'reactivar';
            case 'SUSPENDIDO': return 'suspender';
            case 'FINALIZADO': return 'finalizar';
            case 'CANCELADO': return 'cancelar';
            default: return 'modificar';
        }
    }
    
    async function cambiarEstadoContrato(id, estado) {
        try {
            const response = await fetch(`/contratos/${id}/estado`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({ estado: estado })
            });
            
            const result = await response.json();
            
            if (result.success) {
                location.reload();
            } else {
                alert('Error: ' + result.message);
            }
        } catch (error) {
            console.error('Error:', error);
            alert('Error al cambiar el estado del contrato');
        }
    }
    
    async function eliminarContrato(id) {
        try {
            const response = await fetch(`/contratos/${id}`, {
                method: 'DELETE',
                headers: {
                    'Content-Type': 'application/json',
                }
            });
            
            const result = await response.json();
            
            if (result.success) {
                window.location.href = '/contratos';
            } else {
                alert('Error: ' + result.message);
            }
        } catch (error) {
            console.error('Error:', error);
            alert('Error al eliminar el contrato');
        }
    }
});
</script>

</body>
</html>

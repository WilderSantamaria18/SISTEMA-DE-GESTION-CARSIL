<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Registrar Nueva Contraseña | CARSIL Equipos y Servicios</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --primary-color: #003366;
      --secondary-color: #0077b6;
      --accent-color: #FF6B00;
      --light-gray: #f8f9fa;
    }
    body {
      min-height: 100vh;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
      background: url('https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?ixlib=rb-4.0.3') center/cover no-repeat;
    }
    .overlay {
      position: fixed;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 51, 102, 0.7);
      top: 0; left: 0;
      z-index: -1;
    }
    .auth-container {
      max-width: 500px;
      width: 100%;
      margin: 2rem;
      background: rgba(255, 255, 255, 0.3);
      backdrop-filter: blur(12px);
      border-radius: 12px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.15);
      padding: 2rem 2.5rem;
      color: #fff; /* Texto principal en blanco */
    }
    .brand-header {
      text-align: center;
      margin-bottom: 1.5rem;
    }
    .brand-logo {
      height: 80px;
      width: auto;
      margin-bottom: 0.5rem;
    }
    .auth-title {
      margin-bottom: 1.5rem;
      font-weight: 600;
      font-size: 1.4rem;
      text-align: center;
      color: #fff; /* Título en blanco */
    }
    .form-label {
      font-weight: 500;
      margin-bottom: 0.5rem;
      color: #fff; /* Etiquetas en blanco */
    }
    .form-control {
      padding: 0.7rem 1rem;
      border-radius: 8px;
      border: 1px solid #ced4da;
      transition: all 0.3s;
      color: #232323; /* Mantener texto interno oscuro para contraste */
      background-color: #fafafa;
    }
    .form-control:focus {
      border-color: var(--primary-color);
      box-shadow: 0 0 0 0.25rem rgba(0,83,161,0.25);
    }
    .btn-primary {
      background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
      border: none;
      border-radius: 8px;
      padding: 0.7rem 1.5rem;
      font-size: 1rem;
      font-weight: 500;
      transition: all 0.3s;
      color: #fff; /* Botón con texto en blanco */
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0,83,161,0.3);
    }
    .footer {
      margin-top: 2rem;
      color: #f8f9fa;
      text-align: center;
      font-size: 0.95rem;
      opacity: 0.8;
    }
    a.text-decoration-none.fw-semibold {
      color: #fff; /* Enlace en blanco */
      transition: all 0.2s;
    }
    a.text-decoration-none.fw-semibold:hover {
      color: var(--secondary-color); /* Hover en color secundario */
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="overlay"></div>
  <div class="container d-flex flex-column justify-content-center align-items-center min-vh-100">
    <div class="auth-container">
      <div class="brand-header">
        <img src="/img/Carsil.png" alt="Logo" class="brand-logo">
      </div>
      <h5 class="mb-4 text-center auth-title">Ingresa tu nueva contraseña</h5>
      <form method="POST" action="/nueva-clave" autocomplete="off">
        <input type="hidden" name="correo" value="<%= correo %>">
        <input type="hidden" name="usuarioId" value="<%= usuarioId %>">
        <div class="mb-3">
          <label for="nuevaClave" class="form-label">Nueva contraseña</label>
          <input type="password" class="form-control" id="nuevaClave" name="nuevaClave" required>
        </div>
        <button type="submit" class="btn btn-primary w-100 py-2">Guardar contraseña</button>
      </form>
      <div class="text-center mt-3">
        <a href="/login" class="text-decoration-none fw-semibold">
          Volver al login
        </a>
      </div>
    </div>
    <div class="footer">
      &copy; <%= new Date().getFullYear() %> CARSIL Equipos y Servicios. Todos los derechos reservados.
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>